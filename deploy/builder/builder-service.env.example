# Copy the applicable fields into both root-owned mode-0644 files:
#   /etc/diamaneos/builder-source-sync.env
#   /etc/diamaneos/builder-generic-qualification.env
# Replace the placeholders; do not install this example unchanged.
DIAMANEOS_EXPECTED_TOOLS_COMMIT=<exact-reviewed-40-hex-commit>
DIAMANEOS_THERMAL_CHECK=/absolute/path/to/qualified-thermal-check
